import Session from './Session';

function SessionCard() {
    return (
        <div>
            <h1>Today</h1>

            <Session />
            <Session />
            <Session />
        </div>
    );
}

export default SessionCard;